// src/components/Header.js
// (Kept for compatibility; not used in App.js right now)
import React from "react";

const Header = () => {
  return (
    <header className="simple-header">
      <span className="simple-header-title">AgriAssist</span>
    </header>
  );
};

export default Header;
